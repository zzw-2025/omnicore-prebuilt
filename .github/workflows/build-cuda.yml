name: CUDA build boundary

on:
  workflow_call:

permissions:
  contents: read

jobs:
  windows-cuda:
    runs-on: ubuntu-latest
    steps:
      - name: Explain the trusted build boundary
        run: |
          echo 'CUDA source builds run in the private OmniCore workflow on a trusted Windows NVIDIA runner.'
          echo 'This public repository only signs, describes, and distributes verified artifacts.'
